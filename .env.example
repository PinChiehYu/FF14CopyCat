# 覆寫 Cloudflare Worker 代理的網址（src/config.ts）。
# 未設定時：開發模式用 http://localhost:8787（npm run worker:dev），正式建置用已部署的 Worker。
# 複製成 .env.local 使用；例如本機前端直接連已部署的 Worker：
# VITE_API_BASE=https://ff14-copycat-api.ff14-copycat.workers.dev
VITE_API_BASE=
